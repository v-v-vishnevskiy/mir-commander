---
alwaysApply: true
---

- Do not generate documentation until I explicitly ask.
- Do not generate files with examples of usage.
- Do not suggest to run generated files.
- Do not generate tests until I explicitly ask.
- Always run scripts using the virtual environment in the `.venv` directory, as all libraries are installed there.

Tech stack:
- Python >=3.13
- OpenGL >=3.3 and <=4.1 - pyopengl
- Qt6 library - PySide6
- Pydantic 2.x
- numpy

Tests stack:
- pytest

Build stack:
- cython >= 3
- setuptools
